# 카페24 프로덕션 환경 변수 설정
# 이 파일을 .env.production으로 복사하고 실제 값을 입력하세요

# 데이터베이스 설정 (카페24 MySQL 사용 시)
# DATABASE_URL="mysql://사용자명:비밀번호@호스트:3306/데이터베이스명"

# SQLite 사용 시 (카페24에서 권장하지 않음)
DATABASE_URL="file:./prod.db"

# Supabase 설정 (선택사항 - 외부 DB 사용 시)
NEXT_PUBLIC_SUPABASE_URL=your-supabase-project-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# JWT 인증 설정 (반드시 변경하세요!)
JWT_SECRET=your-super-secret-jwt-key-minimum-32-characters-long

# 서버 설정
NODE_ENV=production
PORT=3000

# 사이트 URL (카페24 도메인으로 변경)
NEXT_PUBLIC_SITE_URL=https://your-domain.cafe24.com
